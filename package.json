{
  "name": "root",
  "private": true,
  "packageManager": "pnpm@9.9.0",
  "engines": {
    "node": ">=20.10"
  },
  "scripts": {
    "package:dev": "pnpm --filter @matthiesenxyz/astrodtsbuilder dev",
    "package:build": "pnpm --filter @matthiesenxyz/astrodtsbuilder build",
    "playground:dev": "pnpm --filter playground dev",
    "dev": "pnpm --stream -r -parallel dev",
    "changeset": "changeset",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "ci:install": "pnpm install --frozen-lockfile",
    "ci:version": "pnpm changeset version",
    "ci:publish": "pnpm changeset publish",
    "ci:snapshot": "pnpx pkg-pr-new publish --pnpm './package/'"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@changesets/cli": "catalog:",
    "@changesets/config": "catalog:",
    "pkg-pr-new": "catalog:",
    "typescript": "catalog:"
  }
}
